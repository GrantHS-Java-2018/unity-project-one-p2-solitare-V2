using System.Collections.Generic;


public class Pile_4 : PileScript
{
    // Start is called before the first frame update
    void Start()
    {
        Stack<Card> pileCards = new Stack<Card>(4);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
